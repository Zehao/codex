# Codex App-Server Debug Project

This directory is the default project workspace used by `../debug.sh`.

Use it for app-server and web debugger experiments so generated files, shell
commands, and thread context do not touch the repository root by default.
